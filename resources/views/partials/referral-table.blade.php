<div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-[11px] uppercase tracking-wider text-slate-500"><tr><th class="px-5 py-3 font-bold">Referral</th><th class="px-5 py-3 font-bold">Route</th><th class="px-5 py-3 font-bold">Urgency</th><th class="px-5 py-3 font-bold">Status</th><th class="px-5 py-3"></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($referrals as $referral)<tr class="transition hover:bg-brand-50/40"><td class="px-5 py-4"><a href="{{ route('referrals.show', $referral) }}" class="font-bold text-brand-700 hover:text-brand-900">{{ $referral->referral_number }}</a><p class="mt-1 text-xs text-slate-500">{{ $referral->patient?->full_name ?? 'Patient record' }}</p></td><td class="px-5 py-4 text-xs text-slate-600">{{ $referral->referringHospital?->short_name ?? $referral->referringHospital?->name }} <span class="text-accent-500">→</span> {{ $referral->receivingHospital?->short_name ?? $referral->receivingHospital?->name }}</td><td class="px-5 py-4"><span class="urgency-pill urgency-{{ $referral->urgency->value }}">{{ $referral->urgency->label() }}</span></td><td class="px-5 py-4"><span class="status-pill status-{{ $referral->status->value }}">{{ $referral->status->label() }}</span></td><td class="px-5 py-4 text-right"><a href="{{ route('referrals.show', $referral) }}" class="font-bold text-brand-600">View</a></td></tr>@empty<tr><td colspan="5" class="px-5 py-16 text-center text-sm text-slate-500">No referrals in this workspace yet.</td></tr>@endforelse</tbody></table></div>